#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum Tajia {
    /// the right next to the one's seat
    Xiajia,
    /// the one across from the one's seat
    Duimian,
    /// the left next to the one's seat
    Shangjia,
}
